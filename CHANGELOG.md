# Changelog

## Unreleased

- 전체 코드 검수: 구버전 DB 마이그레이션, 백업 검증·교체, ZIP 경계, 편집 중 재가져오기, 빠른 캡처·창 생성·검색 경합, 알림 취소 실패를 보완했습니다.
- 커서 이동의 불필요한 저장과 보고서 수정 시각, 모델 다운로드 중복·크기 제한, 날짜 입력 예외를 수정하고 경계 테스트를 추가했습니다.
- 검수 범위·결과와 미해결 항목은 `docs/release-validation.md`에 기록했습니다.

- 메모 저장이 성공한 뒤 메모리 상태를 반영하고, 늦은 편집 저장이 삭제된 메모를 되살리지 않도록 조건부 수정합니다.
- 묶음 작업을 별도 서비스로 분리하고, 작업 토큰과 변경 번호로 다른 창의 후속 변경을 덮어쓰는 실행 취소를 방지합니다. 실패한 일괄 취소는 변경 번호까지 되돌려 재시도할 수 있습니다.
- 전체 보기의 묶음 작업 실패·충돌 안내와 재시도 처리를 추가합니다.

## 0.0.1 — 2026-09-05 (지인 테스트 배포)

- 메모 아래 ‘연결·묶음’에서 추천 여부와 관계없이 메모를 검색해 참고 연결을 설정하고, 묶음을 만들거나 이동·해제할 수 있습니다. 전체 보기에서는 여러 메모를 선택해 기존 묶음에 한 번에 넣거나 뺄 수 있습니다.
- 정리 화면에서 마지막 변경을 실행 취소할 수 있습니다. 새 묶음 만들기를 취소하면 이전 소속을 복원하고, 여러 메모의 소속 복원은 한 트랜잭션으로 처리합니다.

- 기본 Small 모델의 묶음 추천을 보완합니다. 기존 추천 밖에서 서로를 가장 가까이 보는 메모와 세 번째 메모의 뒷받침을 확인해 추가 묶음을 제안합니다. 업무 예제 50개·새 검증 44개와 기존 평가 데이터의 비교 결과를 기록합니다.

- 전체 보기에 ‘묶음 / 관계’ 전환을 추가합니다. 저장한 참고 관계와 개별 메모의 관련 추천을 중복 없이 모아 보고, 메모 열기·유지·해제·추천 숨기기를 제공합니다. 묶음 소속은 바꾸지 않습니다.

- 메뉴바에서 가져오기·내보내기·백업을 실행할 때 파일 선택 창이 숨겨진 검색 창 뒤에 붙어 보이지 않던 문제를 수정합니다. 파일 선택 중 자동 숨김을 막고 종료 시 원래 표시 상태를 복원합니다.

- 전체 보기의 ‘연결 묶음’을 제거하고 이름 있는 묶음을 정리 단위로 통일합니다. 기존 연결·가져온 링크는 참고 관계로 보존하며 연결된 메모도 묶음 추천 대상이 됩니다.
- 메모 아래에서 AI의 ‘관련 메모’를 열어보거나 ‘유지’해 ‘참고 메모’로 남길 수 있습니다. 추천 대상이 같으면 갱신 시 펼친 내용을 유지합니다.

- 전체 보기에 휴지통을 추가합니다. 삭제한 메모를 서랍으로 복원하거나 확인 후 영구 삭제할 수 있습니다. 삭제 관련 DB 변경은 하나의 트랜잭션으로 처리합니다.

- 긴 메모를 문단·토큰 단위로 나누어 전체 내용을 임베딩합니다. 검색은 가장 관련 있는 문단, 그룹 추천은 길이로 가중한 전체 벡터를 사용합니다.
- 수정하지 않은 문단의 임베딩을 재시작 후에도 재사용합니다. 기존 단일 벡터 캐시는 한 번 재계산하며, 512토큰 이내 메모의 모델 입력은 유지합니다.

- 새 자동 묶음은 강한 의미 유사도로 핵심 묶음을 먼저 만들고, 모델별 기준·다른 묶음과의 차이·최소 유사도를 통과한 메모만 추가합니다.
- 개발용 100개와 별도 검증용 60개의 가상 메모를 구분해 오탐 감소와 누락의 절충을 기록합니다.
- 개인 개발 메모·임시 평가 캐시용 `.local-dev/`를 Git에서 제외합니다.

- 스티커를 닫거나 보관·종료하기 전에 대기 중인 편집 저장을 완료합니다.
- 저장 실패 시 내용을 유지하고 재시도할 수 있습니다.
- 모든 메모를 지워도 다음 실행에 환영 메모가 다시 생성되지 않습니다.
- 내용 수정 시각을 창 상태 변경과 분리해 추천·검색·전체 보기의 시간 기준을 보정합니다.
- 기존 묶음에 새 메모를 추가하는 추천, 승인·실행 취소, 지속적인 거절과 해제를 지원합니다.
- ONNX 추론을 별도 isolate로 옮기고, 편집·삭제·모델 전환 뒤의 오래된 결과를 무시합니다.
- 메모 특징과 최대 50,000쌍의 점수를 캐시해 반복 계산을 줄입니다. 내용·시각·벡터 변경 시 해당 결과를 갱신합니다.
- DB 스키마 12로 업그레이드합니다. 기존 수정 시각을 내용 수정 시각으로 이관하며 이전 백업을 읽을 수 있습니다. 새 DB·백업을 이전 앱 버전에서 여는 것은 지원하지 않습니다.
- 가상 메모 100개의 추천 품질 평가, macOS PR·정기 빌드, 기여·보안 안내를 추가합니다.
